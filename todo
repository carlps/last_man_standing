nfl:
  - api
  - sched

 League stuff:
  - create league form
    x create a default rules
    - auto generate passphrase button
  - League detail
    - show details if user in league
    - have a join button if user not in league
    x show rules
    - if admin, allow to edit rule
  - League admin (owner only page)
    - Email invite
    - more stuff
